[pytest]
testpaths = backend/app/tests
addopts = 
    --cov=backend/app
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=80
pythonpath = backend
filterwarnings =
    ignore::DeprecationWarning
